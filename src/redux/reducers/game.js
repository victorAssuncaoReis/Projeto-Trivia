const INITIAL_STATE = {
  assertions: 0,
  score: 0,
  counter: 30,
  isRunning: true,
};

const game = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default game;
